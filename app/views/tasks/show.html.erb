<h1>タスク詳細</h1>
<br>
<table class="table">
  <tr>
    <th>タスク名</th>
    <th>内容</th>
  </tr>
  <tr>
    <td><%=@task.title%></td>
    <td><%=@task.content%></td>
  </tr>
</table>
<h2>登録ラベル</h2>
<% @task.labels.each do |label| %>
<p><%=label.name%></p>
<% end %>

<br>
<%= link_to '一覧', root_path %>
